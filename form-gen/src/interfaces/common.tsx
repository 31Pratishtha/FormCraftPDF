export interface IBasicInfo {
  name: string;
  email: string;
  phone: string;
  dateOfBirth: Date;
  panNumber: string;
}

export interface IAddress {
  street: string;
  city: string;
  state: string;
  pincode: string;
}
